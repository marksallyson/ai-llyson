# Harvest log

Newest first. The `harvest-meetings` skill reads `last_harvest` from the top entry to
determine its window, and prepends a new entry after each run. Keeps the last 30.

<!-- No harvests yet. First run will harvest the last 14 days. -->
